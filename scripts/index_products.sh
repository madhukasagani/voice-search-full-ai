#!/usr/bin/env bash
# Example: index products into Qdrant using the API endpoints in backend
# Ensure OPENAI_API_KEY and QDRANT_URL are set in env
echo "This script is a placeholder. Use the .NET indexing console or implement an HTTP client to call /index endpoint once available."
